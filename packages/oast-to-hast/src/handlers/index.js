import root from './root'
import headline from './headline'
import block from './block'
import {
  bold,
  italic,
  code,
  underline,
  verbatim,
  strikeThrough,
} from './emphasis'

module.exports = {
  root,
  headline,
  block,
  bold,
  italic,
  code,
  underline,
  verbatim,
  strikeThrough,
}
