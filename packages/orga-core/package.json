{
  "name": "orga-core",
  "version": "0.1.0",
  "description": "core of orga, a org-mode syntax parser",
  "author": "Xiaoxing Hu <dawnstar.hu@gmail.com>",
  "license": "MIT",
  "repository": "https://github.com/xiaoxinghu/orgajs/tree/master/packages/orga",
  "files": [ "lib" ],
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib --ignore __tests__",
    "watch": "babel -w src --out-dir lib --ignore __tests__",
    "test": "yarn jest",
    "prepublish": "cross-env NODE_ENV=production npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "cross-env": "^5.1.3",
    "jest": "^22.1.4"
  }
}
