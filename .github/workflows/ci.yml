name: CI

on:
  push:
    path-ignore:
      - 'docs/**'
      - '**/README.org'
      - '**/README.md'
  pull_request:
    path-ignore:
      - 'docs/**'
      - '**/README.org'
      - '**/README.md'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20.x
      - name: run unit tests
        run: |
          yarn install
          yarn build
          yarn test
